services:
  werewolf-agent:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: werewolf-agent-langgraph
    ports:
      - "8082:7861"
    restart: unless-stopped
    environment:
      - APP_ENV=production
